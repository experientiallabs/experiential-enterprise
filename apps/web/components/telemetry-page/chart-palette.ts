// Ink and accent first (the house pair), then the soft-indigo rail and two
// spaced hues; six distinguishable segments covers any realistic pool.
// Home is the Telemetry page (the current surface) so charts share one ramp
// and a model wears one color everywhere; the archived project usage chart
// imports it from here too.
export const SERIES_PALETTE = ["#1a1a1a", "#168a49", "#6366f1", "#d97706", "#0891b2", "#dc2626"];
